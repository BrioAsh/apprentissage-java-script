// Sélectionne l'élément h1 qui a l'id "titrePrincipal"
const titre = document.getElementById('titrePrincipal');
console.log(titre); // Affiche l'élément h1 dans la console

// Séléctionne l'élément div qui a l'id "zoneContenu"
const contenuDiv = document.getElementById('zoneContenu');
console.log(contenuDiv); // Affiche l'élément div dans la console